@page "/account/register"
@using System.ComponentModel.DataAnnotations
@using System.Globalization
@using System.Linq
@using Microsoft.EntityFrameworkCore
@using MudBlazor
@using SmokeExpress.Web.Exceptions
@using SmokeExpress.Web.Validation
@using SmokeExpress.Web.Shared.FormControls
@inject UserManager<ApplicationUser> UserManager
@inject SignInManager<ApplicationUser> SignInManager
@inject NavigationManager Navigation
@inject ILogger<Register> Logger

@* Projeto Smoke Express - Autores: Bruno Bueno e Matheus Esposto *@

<MudPaper Class="register-card mud-elevation-6 p-6">
    <MudText Typo="Typo.h4" Class="mb-1 px-4 pt-4">Cadastre-se</MudText>
    <MudText Typo="Typo.body1" Class="text-muted mb-4 px-4">
        Informe seus dados para aproveitar uma experiência discreta e personalizada.
    </MudText>

    <EditForm Model="_model" OnValidSubmit="RegistrarAsync" class="px-4 pb-4">
        <DataAnnotationsValidator />

        @if (!string.IsNullOrEmpty(_mensagemErro))
        {
            <MudAlert Severity="Severity.Error" Dense="true" Class="mb-4">
                @_mensagemErro
            </MudAlert>
        }

        <MudGrid gutterSize="GutterSize.Small">
            <MudItem xs="12" md="6">
                <MudTextField Label="Nome completo"
                              For="@(() => _model.NomeCompleto)"
                              @bind-Value="_model.NomeCompleto"
                              Immediate="true"
                              variant="Variant.Outlined" />
            </MudItem>
            <MudItem xs="12" md="6">
                <MudTextField Label="E-mail"
                              For="@(() => _model.Email)"
                              @bind-Value="_model.Email"
                              InputType="InputType.Email"
                              Immediate="true"
                              variant="Variant.Outlined" />
            </MudItem>
            <MudItem xs="12" md="4">
                <MudTextField Label="CPF"
                              For="@(() => _model.Cpf)"
                              @bind-Value="_model.Cpf"
                              Immediate="true"
                              HelperText="Digite apenas números."
                              variant="Variant.Outlined" 
                              Mask="@(new PatternMask("000.000.000-00"))"/>
            </MudItem>
            <MudItem xs="12" md="4">
                <MudSelect Label="Gênero"
                           For="@(() => _model.Genero)"
                           @bind-Value="_model.Genero"
                           Dense="true"
                           Placeholder="Selecione..."
                           variant="Variant.Outlined"
                           Required="true"
                           >
                           @foreach (var genero in _generos){
                            <MudSelectItem Value="@genero">@genero</MudSelectItem>
                           }
                </MudSelect>
            </MudItem>
            <MudItem xs="12" md="4">
                <MudDatePicker Label="Data de nascimento"
                               For="@(() => _model.DataNascimento)"
                               @bind-Date="_model.DataNascimento"
                               DateFormat="dd/MM/yyyy"
                               displayFormat="dd/MM/yyyy"
                               Culture="@CultureInfo.GetCultureInfo("pt-BR")"
                               Required="true"
                               variant="Variant.Outlined" />
            </MudItem>
            <MudItem xs="12" md="6">
                <MudTextField Label="Senha"
                              For="@(() => _model.Senha)"
                              @bind-Value="_model.Senha"
                              InputType="@_senhaInputType"
                              Adornment="Adornment.End"
                              AdornmentIcon="@_senhaInputIcon"
                              OnAdornmentClick="ToggleSenhaVisibility"
                              AdornmentAriaLabel="Alternar visibilidade da senha"
                              Immediate="true"
                              variant="Variant.Outlined" />
            </MudItem>
            <MudItem xs="12" md="6">
                <MudTextField Label="Confirmar senha"
                              For="@(() => _model.ConfirmacaoSenha)"
                              @bind-Value="_model.ConfirmacaoSenha"
                              InputType="@_confirmacaoSenhaInputType"
                              Adornment="Adornment.End"
                              AdornmentIcon="@_confirmacaoSenhaInputIcon"
                              OnAdornmentClick="ToggleConfirmacaoSenhaVisibility"
                              AdornmentAriaLabel="Alternar visibilidade da confirmação de senha"
                              Immediate="true"
                              variant="Variant.Outlined" />
            </MudItem>
        </MudGrid>

        <MudStack Spacing="1" Class="mt-3">
            <MudCheckBox Label="Aceito os termos e condições de uso"
                         For="@(() => _model.AceiteTermos)"
                         @bind-Value="_model.AceiteTermos"
                         Required="true" />
            <MudCheckBox Label="Desejo receber ofertas e novidades por e-mail"
                         @bind-Value="_model.ConsentiuMarketing" />
        </MudStack>

        <MudButton variant="Variant.Filled"
                   Color="Color.Primary"
                   Size="Size.Large"
                   FullWidth="true"
                   Class="mt-4"
                   Disabled="@_processando"
                   ButtonType="ButtonType.Submit">
            @(_processando ? "Criando conta..." : "Criar conta")
        </MudButton>

        <MudText Typo="Typo.body2" Class="mt-4">
            Já possui cadastro? <MudLink Href="/account/login">Entre agora</MudLink>
        </MudText>
    </EditForm>
</MudPaper>

@code {
    private readonly RegisterInput _model = new();
    private readonly string[] _generos = new[] { "Masculino", "Feminino", "Outro", "Prefiro não informar" };
    private bool _processando;
    private string? _mensagemErro;

    // Controle de visibilidade das senhas
    private bool _senhaVisivel;
    private bool _confirmacaoSenhaVisivel;
    private InputType _senhaInputType = InputType.Password;
    private InputType _confirmacaoSenhaInputType = InputType.Password;
    private string _senhaInputIcon = Icons.Material.Filled.VisibilityOff;
    private string _confirmacaoSenhaInputIcon = Icons.Material.Filled.VisibilityOff;

    private void ToggleSenhaVisibility()
    {
        if (_senhaVisivel)
        {
            _senhaVisivel = false;
            _senhaInputIcon = Icons.Material.Filled.VisibilityOff;
            _senhaInputType = InputType.Password;
        }
        else
        {
            _senhaVisivel = true;
            _senhaInputIcon = Icons.Material.Filled.Visibility;
            _senhaInputType = InputType.Text;
        }
    }

    private void ToggleConfirmacaoSenhaVisibility()
    {
        if (_confirmacaoSenhaVisivel)
        {
            _confirmacaoSenhaVisivel = false;
            _confirmacaoSenhaInputIcon = Icons.Material.Filled.VisibilityOff;
            _confirmacaoSenhaInputType = InputType.Password;
        }
        else
        {
            _confirmacaoSenhaVisivel = true;
            _confirmacaoSenhaInputIcon = Icons.Material.Filled.Visibility;
            _confirmacaoSenhaInputType = InputType.Text;
        }
    }

    private async Task RegistrarAsync()
    {
        _mensagemErro = null;

        if (_model.Senha != _model.ConfirmacaoSenha)
        {
            _mensagemErro = "As senhas informadas não coincidem.";
            return;
        }

        if (_model.DataNascimento == null || _model.DataNascimento > DateTime.UtcNow.AddYears(-18))
        {
            _mensagemErro = "É necessário ter ao menos 18 anos para criar uma conta.";
            return;
        }

        _processando = true;

        try
        {
            var cpfLimpo = MaskUtils.RemoveMask(_model.Cpf, MaskType.CpfCnpj);

            if (await UserManager.Users.AnyAsync(u => u.DocumentoFiscal == cpfLimpo))
            {
                _mensagemErro = "Já existe um cliente cadastrado com este CPF.";
                return;
            }

            var usuario = new ApplicationUser
            {
                UserName = _model.Email,
                Email = _model.Email,
                NomeCompleto = _model.NomeCompleto,
                DocumentoFiscal = cpfLimpo,
                TipoCliente = "PF",
                DataNascimento = _model.DataNascimento ?? DateTime.UtcNow.AddYears(-18),
                Genero = _model.Genero,
                ConsentiuMarketing = _model.ConsentiuMarketing,
                TermosAceitosEm = _model.AceiteTermos ? DateTime.UtcNow : null,
                EmailConfirmed = true // Confirmação manual para simplificar o fluxo acadêmico
            };

            var resultado = await UserManager.CreateAsync(usuario, _model.Senha);

            if (resultado.Succeeded)
            {
                // Evita definir cookie após a resposta iniciar no Blazor Server.
                // Redireciona para a página de login para o usuário autenticar.
                Navigation.NavigateTo("/account/login?returnUrl=/", forceLoad: true);
                return;
            }

            _mensagemErro = string.Join(" ", resultado.Errors.Select(e => e.Description));
        }
        catch (BusinessException ex)
        {
            Logger.LogError(ex, "Erro ao registrar usuário {Email}: {Message}", _model.Email, ex.Message);
            _mensagemErro = ex.Message;
        }
        catch (ValidationException ex)
        {
            Logger.LogError(ex, "Erro de validação ao registrar usuário {Email}: {Message}", _model.Email, ex.Message);
            _mensagemErro = ex.Message;
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Erro inesperado ao registrar usuário {Email}", _model.Email);
            _mensagemErro = "Não foi possível concluir o cadastro. Tente novamente mais tarde.";
        }
        finally
        {
            _processando = false;
        }
    }

    private sealed class RegisterInput
    {
        [Required(ErrorMessage = "Informe seu nome completo.")]
        [StringLength(200, ErrorMessage = "O nome pode conter no máximo 200 caracteres.")]
        public string NomeCompleto { get; set; } = string.Empty;

        [Required(ErrorMessage = "Informe um e-mail válido.")]
        [EmailAddress(ErrorMessage = "Formato de e-mail inválido.")]
        public string Email { get; set; } = string.Empty;

        [Required(ErrorMessage = "Informe seu CPF.")]
        [CustomValidation(typeof(InputValidation), nameof(InputValidation.ValidateCpfCnpj))]
        public string Cpf { get; set; } = string.Empty;

        [Required(ErrorMessage = "Informe sua data de nascimento.")]
        [DataType(DataType.Date)]
        public DateTime? DataNascimento { get; set; }

        [Required(ErrorMessage = "Informe uma senha válida.")]
        [StringLength(100, MinimumLength = 8, ErrorMessage = "A senha deve possuir ao menos 8 caracteres.")]
        [DataType(DataType.Password)]
        public string Senha { get; set; } = string.Empty;

        [Required(ErrorMessage = "Confirme a senha.")]
        [DataType(DataType.Password)]
        public string ConfirmacaoSenha { get; set; } = string.Empty;

        [Required(ErrorMessage = "Selecione seu gênero.")]
        public string Genero { get; set; } = string.Empty;

        [CustomValidation(typeof(InputValidation), nameof(InputValidation.ValidateAceiteTermos))]
        public bool AceiteTermos { get; set; }

        public bool ConsentiuMarketing { get; set; }
    }
}
